services:
  ChannelRepository:
    module: src.backoffice.contexts.channel.channel.infrastructure.persistence.postgresql.channel_repository
    class:
      name: PostgreSQLChannelRepository
      arguments:
        - !ref Database.Session

  ContentRepository:
    module: src.backoffice.contexts.channel.content.infrastructure.persistence.postgresql.content_repository
    class:
      name: PostgreSQLContentRepository
      arguments:
        - !ref Database.Session
