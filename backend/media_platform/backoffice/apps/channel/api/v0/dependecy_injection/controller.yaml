services:
  HealthCheckController:
    module: media_platform.backoffice.apps.channel.api.v0.controllers.health_check_controller
    class:
      name: HealthCheckController

  ChannelsGetController:
    module: media_platform.backoffice.apps.channel.api.v0.controllers.channels_get_controller
    class:
      name: ChannelsGetController
      arguments:
        - !ref ChannelSearcher

  ChannelGetController:
    module: media_platform.backoffice.apps.channel.api.v0.controllers.channel_get_controller
    class:
      name: ChannelGetController
      arguments:
        - !ref ChannelFinder

  ChannelPostController:
    module: media_platform.backoffice.apps.channel.api.v0.controllers.channel_post_controller
    class:
      name: ChannelPostController
      arguments:
        - !ref ChannelCreator

  ChannelPutController:
    module: media_platform.backoffice.apps.channel.api.v0.controllers.channel_put_controller
    class:
      name: ChannelPutController
      arguments:
        - !ref ChannelUpdater

  ChannelDeleteController:
    module: media_platform.backoffice.apps.channel.api.v0.controllers.channel_delete_controller
    class:
      name: ChannelDeleteController
      arguments:
        - !ref ChannelEliminator

  ContentsGetController:
    module: media_platform.backoffice.apps.channel.api.v0.controllers.contents_get_controller
    class:
      name: ContentsGetController
      arguments:
        - !ref ContentSearcher

  ContentGetController:
    module: media_platform.backoffice.apps.channel.api.v0.controllers.content_get_controller
    class:
      name: ContentGetController
      arguments:
        - !ref ContentFinder

  ContentFileGetController:
    module: media_platform.backoffice.apps.channel.api.v0.controllers.content_file_get_controller
    class:
      name: ContentFileGetController
      arguments:
        - !ref ContentFinder

  ContentPostController:
    module: media_platform.backoffice.apps.channel.api.v0.controllers.content_post_controller
    class:
      name: ContentPostController
      arguments:
        - !ref ContentCreator

  ContentPutController:
    module: media_platform.backoffice.apps.channel.api.v0.controllers.content_put_controller
    class:
      name: ContentPutController
      arguments:
        - !ref ContentUpdater

  ContentDeleteController:
    module: media_platform.backoffice.apps.channel.api.v0.controllers.content_delete_controller
    class:
      name: ContentDeleteController
      arguments:
        - !ref ContentEliminator

  ChannelRatingGetController:
    module: media_platform.backoffice.apps.channel.api.v0.controllers.channel_rating_get_controller
    class:
      name: ChannelRatingGetController
      arguments:
        - !ref ChannelFinder
        - !ref ContentSearcher